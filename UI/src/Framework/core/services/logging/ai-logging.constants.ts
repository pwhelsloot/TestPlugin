export class AiLoggingConstants {
  static readonly Prefix: string = 'APDEX';
  static readonly FirstPaint: string = `${AiLoggingConstants.Prefix} First Paint`;
  static readonly Ready: string = `${AiLoggingConstants.Prefix} Ready`;
  static readonly Api: string = `${AiLoggingConstants.Prefix} API`;
}
